# Build plan — v0.2.0 (autonomic-loop substrate)

The phase that turns Audri from a voice-captured wiki into a system that *reflects on, maintains, and proactively grows* its own knowledge of the user. Synthesizes two parallel research passes — `notes/llm-wiki-ecosystem-survey.md` (Karpathy LLM-wiki ecosystem + Paco Cantero's Mindset) and `notes/openclaw-evaluation.md` (memory-wiki, active-memory, dreaming) — into one coherent build target.

**Source of truth:** the design rationale lives in the two notes above. This doc commits to specific shipping work and flags critical decision points for resolution before coding begins.

**Legend**

- ✅ — done
- ✴️ — partial / in progress
- ⏺️ — open / not started
- ⛔ — blocked
- ❓ — decision point — must resolve before this item is cleared for coding

---

## Phase scope

The organizing target is the **proactive gap-filling autonomic loop** (per `project_proactive_gap_filling` memory): Audri reflects on each conversation, identifies gaps in its knowledge of the user, and surfaces questions in future conversations to fill them. The loop also carries the reframed proactive-information channel — agents introducing edifying, context-aware content unprompted (not curriculum-driven concept-mastery; that's reserved for V1+ specialist/tutor agents).

The loop has four moving parts:

1. **Structured-claims substrate** — every fact carries enough metadata for contradiction-detection, freshness, and contestability. Schema invariant the rest of the phase assumes.
2. **Agent-scope ingestion does the reflection** — the existing per-persona ingestion pass extends to also emit (a) open questions and (b) info-to-share candidates per call. No separate scheduled `gap_analysis` plugin in v0.2; the per-call cadence is sufficient and avoids batch-vs-stream complexity.
3. **A queue of agent-initiated items** — pending questions and info-shares live in one table, scoped per-persona, read by the call-side prompt composer.
4. **Call-side prompt composer reads the queue** — weaves selected items into the persona prompt as natural curiosity / sharing, bounded by saturation rules.

Plus one parallel UX surface (the Agents homescreen tile) and one supporting plugin (vault-first delta research, generalizing the existing `research` plugin). Connectors (Path 1 from the OpenClaw eval) are **deferred to v0.3** — different work, different mental model, runs as a separate track when this phase is sequenced.

The phase also includes three secondary tracks that don't sit on the autonomic-loop critical path but ship together with it: **mobile reliability** (sync verification + pull-to-refresh + Wiki pending indicator — making the existing data layer trustworthy enough to anchor everything else), **onboarding script rework** (recasts the call from life-history-first to current-life-first, and seeds new capability promises — daily briefs, connectors, dreaming — that downstream phases must honor), and a **decision point on per-turn call ingestion** (a potentially significant architectural shift that interacts with the Wiki pending indicator and with the deferred mid-call write tool).

## North-star UX (pinned)

Every item below should be checked against these. Phrasing is Paco Cantero's; the vision is wholly Audri's.

1. I speak naturally, the system handles everything else.
2. Every thought, decision, and interaction is captured without friction.
3. The system sees patterns across every area of my life, not just one app.
4. Coaching and insights arrive without me asking for them.
5. It gets smarter every single day, not just bigger.

This phase primarily targets bullets **#4** (loop closes via agent-initiated items) and **#5** (claims model + delta research = real compounding). Bullets #1 and #2 are already satisfied by the existing voice surface; bullet #3 lands as a side-effect of the per-persona reflection plus future cross-persona synthesis (deferred).

---

## Decision points — resolve before coding

These need a Socratic pass before any of the dependent items below get scheduled. Listed in roughly the order they need to be answered.

### ❓ DP-1. Active-memory pattern (OpenClaw lift) — adopt, defer, or ignore?

**What it is.** OpenClaw's `active-memory` is a blocking pre-reply sub-agent: before the main response, a configurable retrieval pass runs against memory with bounded `timeoutMs`, configurable `queryMode` (`message` / `recent` / `full`) and `promptStyle` (`balanced` / `strict` / `recall-heavy` / `preference-only`), and injects the result as hidden system context. Audri's call-side preload + Flash retrieval is the closest analog we have today; the OpenClaw pattern *generalizes* it into a config-driven blocking pass.

**Why it might matter for v0.2.** The call-side prompt composer that reads the open-questions queue is the same plumbing that feeds retrieval into the persona prompt. If we generalize Flash into the active-memory shape *first*, gap-questions become "one more channel" through the same pass. If we don't, we end up with two parallel mechanisms.

**Why it might not.** Our Flash retrieval works. Generalizing it adds surface area before we have evidence we need the configurability. The right time may be V1+ when multiple personas demand different retrieval profiles.

**Resolution needed:** a Socratic discussion of what the existing Flash retrieval does today, where it falls short, and whether the active-memory configurability genuinely helps or is over-engineering for v0.2. Until resolved, treat the call-side prompt composer as an extension of Flash, not a replacement for it.

### ✅ DP-2. Dreaming (weekly synthesis pass) — RESOLVED 2026-05-09: adopt as designed, deferred to v0.3

**Naming clarification.** OpenClaw's `dreaming` and the survey's `weekly_synthesis` (insight #1's third hygiene plugin) are the same thing. "Dreaming" is the user-facing brand; the underlying mechanism is weekly synthesis with a weighted scoring rubric and a user-readable diary.

**Resolution (Max, 2026-05-09):** adopt the OpenClaw design wholesale — configurable cadence (default weekly), Light / REM / Deep phase split, six-weight scoring rubric (frequency / relevance / query-diversity / recency / consolidation / conceptual-richness), "Dreaming" branding. Bundled as the first **installable plugin** (not always-on). On install, a top-level user-scoped wiki page `Dreams` is seeded. Each dream/insight produced by the pass is its own `wiki_pages` row of `kind = 'dream'`, parented under `Dreams`. Users can review each dream and either promote (move parent_page_id to a target main-wiki page — no section rewrite needed) or archive (uses the new Archive backlog feature). Default unreviewed-dream auto-archive after 2 weeks (configurable).

**Phase placement.** Deferred to **v0.3**, paired with the Connectors track. v0.3 becomes "installable plugins" as a coherent theme: connectors + dreaming + the install-flow infrastructure they share + the Archive feature dreams need. See `build-phases/v0.3.0.md`.

**Implication for v0.2's onboarding script.** The dreaming line is **cut** from the new self-introduction script (#12b) for v0.2's ship. Restored in v0.3 when dreaming actually lands. The script in v0.2 promises four capabilities (capture / research / briefs / connectors); the dreaming line returns in v0.3's onboarding-script revision.

**Open design points (resolved as MVP defaults, revisit at v0.3 spec time):**
- `kind = 'dream'` confirmed (vs. `kind = 'note'` with parent = dreams) — gives clean Dreams-plugin UI filter and makes promotion a legible state transition (`dream` → `concept`/`note`).
- Promotion target picker: Option A for MVP (agent suggests parent based on dream content + existing wiki, user confirms or overrides), Option C for V2+ (full agent-suggest + user-browse + override).
- Unreviewed dream auto-archive: configurable, default 2 weeks.
- Cadence config: configurable per-user, default weekly.

### ❓ DP-3. Dashboard shapes (OpenClaw lift) — what wiki-health surfaces do we emit?

**What it is.** OpenClaw's `memory-wiki` ships dashboards as auto-generated markdown reports: `contradictions.md`, `stale-pages.md`, `low-confidence.md`, `open-questions.md`, `person-agent-directory.md`, `relationship-graph.md`. The `wiki_health_audit` plugin (#1 from the survey) was originally going to emit dashboard-style outputs; the question is *which* dashboards, and whether they're agent-readable artifacts, user-visible UI surfaces, or both.

**Why it might matter for v0.2.** The Agents homescreen tile (committed below) needs *something* to display. Open-questions is obviously one of those things — but the others (contradictions, stale, low-confidence) require the structured-claims substrate to be in place before they can be computed, and the user-visible surface design hasn't been spec'd.

**Why it might not.** v0.2's headline is the autonomic loop, not wiki-health observability. Dashboards beyond "open questions for this persona" may belong in a later phase focused on user-facing wiki transparency.

**Resolution needed:** decide which dashboards are in scope for v0.2. My read: only **open-questions** (per persona) is load-bearing for the loop; the rest are nice-to-have observability that fits better in a later transparency-focused phase. Confirm or push back.

### ❓ DP-4. Agents homescreen tile / plugin — UX design

The pending question queue + proactive info-shares need a UX home. The user identified the Agents plugin/homescreen tile as the right surface, but the tile doesn't exist yet. Open questions: Does this tile show *all* personas, or one selected? Does the user dismiss/snooze questions here, or only see what the persona is "thinking about"? Is this a passive transparency surface (Audri shows what it's curious about) or an active edit surface (user can add/dismiss questions directly)? What's the Transparency-vs-Proactiveness balance?

**Resolution needed:** design pass + spec doc (probably `specs/agents-plugin.md`). Block on this before shipping the loop end-to-end.

### ❓ DP-5. Single `agent_open_items` table or split tables for questions vs. info-shares?

The reframed #6 (proactive info-introduction) and the gap-filling questions are both forms of agent-initiated content: same delivery mechanism, same per-persona scoping, same saturation rules. They differ in lifecycle (questions get *answered*; info-shares just get delivered or expired) and in source (questions come from gaps; info-shares come from synthesis on existing knowledge).

Two options:
- **Single table** with `kind` enum (`question | info_share`) and a `status` enum that covers both lifecycles. Cleaner, simpler, shares all infrastructure.
- **Two tables** — `agent_open_questions` and `agent_proactive_shares`. More explicit lifecycles, slightly more code but each table is simpler.

**Resolution needed:** pick one. My lean: single table with `kind` enum for v0.2 simplicity; split later if lifecycles diverge.

### ❓ DP-6. Claim-model migration scope (where does the schema live?)

User confirmed big-bang on the claim-model migration (`status`, `confidence`, `evidence[]`, `last_refreshed_at`, `valid_at`). Open: do these fields live on `extracted_claims`, `wiki_sections`, or both? Survey doc's open-question #3 floated "claims first, sections inherit when promoted." Worth confirming the exact shape before the migration is written.

**Resolution needed:** schema sketch + review. One migration, one chance to get it right; the rest of v0.2 builds on this.

### ❓ DP-7. Question-quality prompt engineering — design risk concentration

The hardest part of the loop isn't schema or scheduling; it's making questions feel like genuine curiosity, not interrogation. The agent-scope ingestion prompt that emits questions is one half (structured candidate); the call-side prompt composer that delivers them is the other (natural delivery). Both need iterative prompt engineering against real call recordings.

**Resolution needed:** budget for this. Suggest: don't ship the loop end-to-end on first cut. Ship the substrate + queue + UX surface first, populate the queue manually for a week, observe; *then* wire the agent-scope generator and the prompt composer. This separates schema risk from prompt-engineering risk. **Note:** Max deferred the broader call-prompt-architecture evaluation to *after v0.2 ships* (2026-05-09). DP-7 is narrower than that — it's about question-delivery prompt-engineering specifically. The full architecture review picks up post-phase.

### ❓ DP-8. Per-turn call ingestion — adopt, defer, or hybrid?

**What it is.** Today, ingestion is one batch pass after the call ends: full transcript → Phase 1 (Flash retrieval) → Phase 2 (Pro fan-out) → Phase 3 (transactional commit), with a per-user FIFO Graphile queue. Per-turn would split this: each turn (or each pair of user+agent turns) flows into its own ingestion pass with much narrower context. New schema would need a `call_turns` table as a child of `call_transcripts` (turn-level rows with role, text, timestamps, optional per-turn ingestion status).

**Why it might matter.**
- *Latency:* the Wiki pending state shrinks from "post-call ingestion duration" (currently tens of seconds to minutes) to "single-turn ingestion duration." Pairs naturally with the pending indicator (item #11 below) — could obviate the need for it for steady-state turns.
- *User-felt freshness:* mid-call references in the wiki app become live. Aligns with north-star bullet #2 ("captured without friction").
- *Eventually enables* the deferred mid-call write tool — turns are already individually committed, so the question becomes "can the call-side reasoning agent see them?" rather than "should we even commit mid-call?"
- *Cheaper Pro context per pass* — narrower input, smaller token spend per pass even if more passes run total. Net cost direction not obvious without modeling.

**Why it might not.**
- *Loss of batch coherence.* Pro currently sees the *whole* call when fan-out runs — it can identify cross-turn synthesis, resolve contradictions stated five turns apart, and pick the right hierarchy when a topic emerges over multiple turns. Per-turn loses that, or has to reconstruct it via re-reading prior commits, which is its own cost.
- *Write/retract risk.* User says "I work at Anthropic" turn 1, "actually scratch that, I work at Google" turn 5. Batch sees both and writes Google. Per-turn writes Anthropic, then has to know to retract. The bi-temporal claim model (DP-6) helps but doesn't solve.
- *Cost direction is genuinely unclear* — more passes means more LLM calls, even if each is cheaper. Could be net cheaper, net more expensive, or wash. Needs modeling against a real transcript.
- *Increased schema + queue complexity.* Per-user FIFO queue assumes one job per call; per-turn means N jobs per call with intra-call ordering invariants. Graphile can do this with job-key-mode but it's a real change.
- *Hybrid options exist.* The batch-or-per-turn binary may be a false dichotomy. Hybrid options worth naming:
  - *Per-turn lightweight + post-call full pass:* per-turn does claim extraction + simple updates only; post-call full Pro fan-out still runs for synthesis, hierarchy, and tombstoning.
  - *Adaptive batch:* commit every N turns or every M seconds of conversation, not every turn.

**Resolution needed:** Socratic pass on whether this lands in v0.2 at all, and if so in what form. My read: **defer the architectural change to v0.3 or later**, ship the pending indicator (item #11) as a UX-side fix that works regardless of underlying ingestion shape, and do the cost/coherence modeling on per-turn separately. This DP closes either as "deferred" with a follow-up note, or "adopt as hybrid in v0.2" if the modeling work shows clear wins.

---

## In scope (committed)

### Substrate

1. ⏺️ **Structured-claims schema migration (big-bang)** — depends on **DP-6**. Adopt the OpenClaw memory-wiki claim model fields (`status: 'supported' | 'contested' | 'rejected'`, `confidence`, `evidence[]`, `last_refreshed_at`) and our bi-temporal extension (`valid_at` separate from `recorded_at`). Migration generates from the schema diff; backfill defaults to `recorded_at` for existing rows. Phase 2 fan-out prompt updated to extract validity + confidence when stated. Source-of-truth field placement decided at DP-6.

2. ⏺️ **Tiered profile maturity + personCard fields on `person` pages** — derived `maturity` enum (`stub | moderate | full`) over inbound junction-row count (COG's tiering), plus structured fields from OpenClaw's `personCard` (handles, socials, timezone, ask-for, avoid-asking-for, privacy). Call-side context-loader uses tier to decide injection depth; UI surfaces tier on the person page. Generalizable to `project`, `concept`, `org` once the pattern is proven.

### The autonomic loop

3. ⏺️ **`agent_open_items` table** — depends on **DP-5**. Per-persona queue of agent-initiated content. Fields (single-table version): `id`, `user_id`, `agent_id`, `kind` (`question | info_share`), `topic`, `body_text`, `priority`, `status` (`pending | surfaced | answered | dismissed | expired`), `created_by_task_id` (the agent-scope ingestion run that produced it), `created_at`, `surfaced_at`, `resolved_at`, `cross_domain_links` (optional refs to other agent-scope items).

4. ⏺️ **Agent-scope ingestion extended to emit open items** — depends on **DP-7**. The existing per-persona ingestion pass (see `specs/agent-scope-ingestion.md`) is extended to output, alongside its current persona-private observations, two new artifact types per call: (a) candidate questions ("what does this persona want to ask next time"), (b) candidate info-shares ("what does this persona want to proactively introduce next time, given new context"). Both flow into `agent_open_items`. **No separate `gap_analysis` plugin run** — the per-call cadence drives generation; questions are tied to recent context. Cross-call/across-time synthesis comes via the persona's accumulated agent-scope wiki, not via batch reprocessing.

5. ⏺️ **Call-side prompt composer reads the queue** — depends on **DP-1**, **DP-7**. The 7-layer system prompt at `POST /calls/start` gains a new layer (or augments an existing one — exact placement TBD with DP-1) that pulls the top-K pending items for the active persona, ranked by priority + recency, and weaves them into the persona prompt with explicit instructions for natural delivery (questions as curiosity, info-shares as relevance). Saturation budget enforced in this layer: K items per call, weekly-budget-per-persona, explicit `dismissed` items excluded.

6. ⏺️ **Post-call item resolution in fan-out** — extends the existing post-call ingestion fan-out: for each pending item the persona had at call start, attempt resolution against the transcript. Questions matched by topic + answer-detection prompt → status `answered`. Info-shares matched by "did the persona deliver it" → status `surfaced` (and possibly `answered` if the user engaged). Items that linger past N calls → status `expired` (with a hygiene sweep — see #8 below). **No mid-call write tool in v0.2** (deferred trade — confirmed in the survey's headline-extension section).

### UX surface

7. ⏺️ **Agents homescreen tile / plugin** — depends on **DP-4**. New plugin module + home-screen tile per the existing `apps/mobile` plugin pattern. Surfaces the per-persona open-items queue as a Transparency expression of what each persona is "thinking about." Dismiss/snooze interactions, queue browsing, optional manual question seeding (decided in DP-4).

### Supporting plugin work

8. ⏺️ **Vault-first delta research** — survey insight #2. Refactor the existing `research` plugin handler to (a) start with a `search_wiki` pass scoped to the topic, (b) inject retrieved sections into the research prompt as `## Existing knowledge`, (c) emit gap-targeted external queries, (d) produce a delta report referencing existing `wiki_pages`/`wiki_sections` UUIDs that should be updated/contradicted, (e) feed the delta into the same fan-out → transactional commit path used by call-side ingestion. Pure prompt revision + flow change; no schema work beyond the substrate above.

9. ⏺️ **Hygiene sweep — item expiration** — minimal version of the broader `wiki_health_audit` plugin (full version deferred per DP-3). One scheduled job per user that expires `agent_open_items` past a `max_age_calls` or `max_age_days` threshold and emits a per-persona "items expired" log. Uses the existing `agent_task_dispatch` infra. The full dashboard family (contradictions, stale, low-confidence) is gated on DP-3.

### Mobile reliability

10. ⏺️ **RxDB ↔ Supabase sync verification + pull-to-refresh** — Max reports that mobile sometimes appears to need a full app restart to trigger a data refresh; the autonomic loop's UX trustworthiness depends on real-time sync working correctly. Two parts:
    - *Verification:* audit the RxDB replication state under realistic conditions (foreground/background transitions, network drops, post-ingestion arrivals from server, RLS-scoped table updates). Identify whether the issue is replication lapse, RxDB query reactivity, or stale React component subscriptions. Likely starting points: `apps/mobile/lib/rxdb/replication.ts`, RxDB live-query hooks, Supabase realtime channel state.
    - *Pull-to-refresh affordance:* every plugin overlay's primary scroll gets a `RefreshControl` that triggers a manual replication pull cycle. Belt-and-suspenders: even after the replication audit, the user should always have an explicit recourse. Pairs with the pending indicator (#11) — together they make ingestion legible end-to-end.

11. ⏺️ **Wiki pending indicator** — Visible state in the Wiki plugin overlay during the gap between "call ended" and "ingestion + sync complete." Tells the user *we know you're expecting new content here, we're working on it.* Depends on knowing call → ingestion → sync lifecycle on the client. Implementation:
    - Server already enqueues `ingestion-${user_id}` jobs on call end; `agent_tasks` rows track status. Subject to the pending-artifact-placeholders pattern from v0.2.1 item #10 (which requires syncing `agent_tasks` to mobile via RLS + realtime publication migration).
    - Client subscribes to relevant `agent_tasks` (kind=`ingestion`, status in `pending|running`) for the active user, plus a "last-replicated-at" watermark per call.
    - Indicator clears when (a) the ingestion task reports `succeeded` AND (b) any wiki_pages/wiki_sections updates with `created_at >= call_ended_at` have replicated locally.
    - **Schema dependency:** the same `agent_tasks → RxDB` migration noted in v0.2.1 item #10 is the prereq. Either pull that migration forward into v0.2 or own it as part of this item. Either way, decide before coding.
    - **Interaction with DP-8:** if per-turn ingestion lands, this indicator's lifecycle gets shorter and may eventually be moot for steady-state turns. Ship it for v0.2 regardless — the UX win stands under either ingestion model.

### Prompt engineering

12a. ⏺️ **Conversational Routing — full refactor** — promoted in from v0.1.1 closeout (2026-05-09). Phase 1 scaffolding shipped + field-tested in v0.1.1, but Max's read on the resulting call dynamics was unsatisfying enough that further iteration on the existing architecture didn't seem worth the cost. The v0.2 substrate gives the refactor real new material to work with: the structured-claims model (#1) means the prompt can route by claim *kind* rather than by surface intent; the agent-scope queue (#3, #4, #5) means the prompt composer is already being rebuilt for queue-driven items, so routing can ride on top of that work; the bi-temporal `valid_at` (DP-6) lets the model distinguish current-state vs. historical claims, which is one of the routing dimensions Phase 1 lacked. **Sequenced LATE in the phase**, after the substrate + autonomic loop land — see sequencing sketch below. Mirror updated to `specs/conversational-routing.md` when the refactor lands. **Out of scope for this item**: the full call-prompt-architecture review (which audits the entire 7-layer system prompt structure end-to-end) — that remains a post-v0.2 follow-up.

12b. ⏺️ **Onboarding script rework** — `apps/server/src/calls/system-prompt.ts → composeOnboardingScaffolding`. Two specific revisions:
    - **Expectation-setting / interview opener.** Today the agent leads with life-history-style questions; field-test feedback (Max, 2026-05-09) is that this isn't the right entry point. Invert the framing: *"the best way to start is to kind of just, start. Tell me about what's going on in your life right now — it could be your relationships, a project you're working on, a topic you want to learn more about, anything really. I'll try and find ways of helping out where I can."* Current-life-first lets the agent immediately demonstrate value (capture + offer help) instead of front-loading background.
    - **Self-introduction (capability framing).** Add: *"Think of me like your second brain. You tell me stuff and I remember it for you. When the call ends, I'll record everything we talked about in your personal notes. I can do other stuff too — like research topics, create daily briefs or weekly recaps. You can plug me into your email, schedule, and meeting apps to help with work."*
    - **Dreaming line cut for v0.2** (per DP-2 resolution 2026-05-09). The original draft included *"I can even Dream — that's where I mull over your notes in my sleep and come up with new synthesis and connections that I can surface to you."* This is restored in v0.3's onboarding-script revision when Dreaming actually ships. Don't reintroduce until v0.3.
    - **Capabilities-promised tracking.** This v0.2 script seeds four user-facing capability promises that downstream phases must honor — see "Promises seeded by onboarding" under deferred below.
    - Field-test in the v0.2 dogfood loop. Mirror to `specs/onboarding.md`.

### Reframe captured (no separate work item)

- **Reframed survey insight #6** — agent personas should *proactively introduce information-rich, edifying content that builds on the user's personal context and interests*, **not** linear progression through a curriculum toward concept-mastery. Curriculum framing is reserved for V1+ specialist/tutor agents. Implementation: the info-shares half of `agent_open_items` (item #3 + #4), not a separate `concept_taught` table.

### Replaced from the survey

- **Survey insight #5 (two-output rule, voiced)** — replaced. Mid-call verbal acknowledgments would be overbearing; instead, ship a single **post-call ingestion summary report** the user can review on the home screen ("From last call I added 3 sections, updated your `Anthropic` page, and answered Audri's question about your sister"). This is a UI-only surface read against `wiki_sections.created_at`/`updated_at` + `agent_open_items.resolved_at` joined to the call session — no new schema. Slot this in once the substrate is in place; doesn't block the loop.

---

## Out of scope / deferred

- **Concept-mastery progression** (survey #6 original framing) — V1+ specialist/tutor agents only.
- **Mid-call write tool / voiced two-output ack** — deferred trade per survey + this doc.
- **Connectors plugin family** (gogcli / notcrawl / remindctl) — Path 1 from OpenClaw eval — deferred to v0.3 or runs as a parallel track. Different mental model; doesn't compete with the loop work. **Promised in onboarding script (#12)** — see tracking note below.
- **Active-memory generalization** of Flash retrieval — gated on DP-1.
- **Weekly synthesis pass / dreaming** — gated on DP-2. (These were two entries previously; collapsed into one — they're the same plugin under different names.) **Promised in onboarding script (#12b)** — DP-2 must close one way or the other before that script ships.
- **Wiki-health dashboards beyond open-questions** (contradictions / stale / low-confidence / etc.) — gated on DP-3.
- **Cross-persona synthesis** (one persona reading another's agent-scope) — V1+.
- **Scheduled `gap_analysis` plugin** as a separate run — superseded by per-call agent-scope ingestion (item #4).
- **Universal `content_index` denormalized table** — explicitly rejected; typed junctions + Postgres FTS get us the same query reach (per survey doc).
- **Per-turn call ingestion + `call_turns` table** — gated on DP-8. My recommendation is to defer the architectural change and ship the pending indicator (#11) as the immediate UX fix.
- **Full call-prompt-architecture evaluation** — Max deferred this to *post-v0.2* (2026-05-09). Distinct from the Conversational Routing refactor (#12a — *in-phase*, sequenced late): the routing refactor targets the routing layer specifically with the new substrate as material; the full architecture review audits the entire 7-layer system prompt structure end-to-end. **Open as a v0.3 follow-up item.**

### Promises seeded by onboarding (tracking)

The reworked v0.2 onboarding script (#12b) tells the user about **four** capabilities (the dreaming line was cut per DP-2 resolution 2026-05-09 — restored in v0.3 when Dreaming ships). We must keep the gap between promise and delivery short enough that the pitch stays honest. Current state per capability:

| Capability promised | State |
|---|---|
| "I record everything we talk about in your personal notes" | ✅ Already shipped (call → ingestion pipeline) |
| "I can research topics" | ✅ Already shipped (`research` plugin); v0.2 sharpens it via vault-first delta |
| "I can create daily briefs or weekly recaps" | ⏺️ Not shipped. Originally survey insight #1 (scheduled hygiene). Deferred from v0.2 in favor of the autonomic loop. Should land v0.3 or v0.4 to honor the promise. |
| "You can plug me into your email, schedule, and meeting apps" | ⏺️ Not shipped. OpenClaw Path 1 connectors. Committed to v0.3 ("installable plugins" theme). |
| ~~"I can Dream"~~ — *cut from v0.2 script* | ⏺️ Not shipped. Committed to v0.3. Restored in v0.3's onboarding-script revision. |

Update this table as each capability lands.

---

## Sequencing sketch

Subject to revision after the decision points are resolved. Each step below is a runnable end-to-end demoable thing, not a sprint. The reliability + onboarding tracks can run in parallel with the loop work — they touch different files and unblock each other only at the dogfood phase.

1. **Resolve decision points** — Socratic pass on DP-1 through DP-8. Update this doc with resolutions. *Block on this before any schema work.* DP-2 (dreaming) needs particular attention because it gates the new onboarding script.
2. **Mobile reliability fix** (#10) — RxDB sync audit + pull-to-refresh. Independent of the loop substrate; lands first because it makes the rest of the v0.2 dogfood loop trustworthy. Also unblocks the schema decision in #11.
3. **`agent_tasks → RxDB` migration** — prereq for both the Wiki pending indicator (#11) and the v0.3 pending-artifact placeholders. Pulled forward from v0.3.
4. **Wiki pending indicator** (#11) — depends on #2 and #3.
5. **Substrate** — claim-model migration (#1) + person-page tier/personCard (#2 of in-scope). One migration, prompt updates to fan-out to populate the new fields, call-side context-loader updated to read tier.
6. **Vault-first delta research** (#8) — uses the new claim model immediately; cheapest standalone validation that the substrate works.
7. **Queue + manual seeding** — `agent_open_items` table (#3), Agents homescreen tile (#7) shows the queue, no auto-population yet. Manually seed a few items per persona; observe how they look in the prompt + on the tile.
8. **Onboarding script rework** (#12) — can run any time after DP-2 closes. If "adopt dreaming," this step blocks on dreaming landing (whichever phase that ends up in); if "defer/ignore," edit the script to drop the dreaming line and ship now.
9. **Auto-population** — agent-scope ingestion extended to emit candidates (#4). Iterate on the prompt against real call recordings until candidates feel right.
10. **Loop close** — call-side prompt composer reads queue (#5) + post-call resolution (#6). Iterate prompt-engineering on natural delivery.
11. **Hygiene + summary surface** — item expiration sweep (#9) + post-call ingestion summary (#5-replacement). Polish.
12. **Conversational Routing refactor** (#12a) — late in the phase, after the loop lands. Refactor the routing prompt layer to leverage the new substrate (claim kinds, queue-driven composer, bi-temporal `valid_at`).

If the loop end-to-end takes longer than expected, ship items 1–7 as a usable v0.2.0 (reliability + substrate + manual queue + UX surface) and let auto-population + delivery + routing-refactor slip into v0.2.1.

---

## Open links

- `notes/llm-wiki-ecosystem-survey.md` — full survey + headline gap-filling extension
- `notes/openclaw-evaluation.md` — OpenClaw integration evaluation; this phase lifts memory-wiki claim model and (pending DP-1/DP-2/DP-3) potentially active-memory + dreaming + dashboards
- `notes/karpathy-llm-wiki.md` — original Karpathy LLM-wiki framing (background)
- `specs/agent-scope-ingestion.md` — current agent-scope ingestion spec; item #4 amends this
- `specs/conversational-routing.md` — locked spec; the prompt composer (#5) interacts with routing
- `specs/agents-plugin.md` — **does not yet exist**; needed for DP-4
- `architecture.md` — system overview; this phase preserves the existing voice + ingestion architecture
- `tradeoffs.md` — log mid-call-write deferral and `agent_open_items` single-vs-split-table decision (after DP-5) here
- `judgement-calls.md` — log the active-memory / dreaming / dashboards resolutions here once DP-1/DP-2/DP-3 close
