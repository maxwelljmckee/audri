# Build plan — v0.5.0 (UX/UI continuation + folded v0.3.0 hangers)

**Status:** opened 2026-05-19 by promoting the original v0.4.0 UX-continuation doc into the v0.5.0 slot, then folding the carry-forward items from v0.3.0 into it. The slot rename made room for a new v0.4.0 anchored on the customization-framework spec + Dreams pull-in (see `specs/customization-framework.md`). This doc carries (a) the original UX-continuation tranche (WYSIWYG, plugin overlay polish, etc.) and (b) the in-flight + open items that didn't ship in v0.3.0 and don't belong to the v0.4.0 customization sprint.

A patch cycle that originally picked up the open UX/UI items pulled out of v0.1.1 to let that patch ship on its visual-identity + prompt-engineering core. **Renumber history:** originally drafted as v0.3.0 → renumbered to v0.2.1 (2026-05-09) when v0.3.0 was claimed by the installable-plugins major-feature theme → renumbered to v0.2.2 (2026-05-11) when the Usage-tracking cycle claimed v0.2.1 → bumped to v0.4.0 (2026-05-12) to make room for additional backend cycles → **promoted to v0.5.0 (2026-05-19)** to make room for the customization-framework v0.4.0 sprint. Distinct from v0.2.0 (autonomic-loop substrate — schema + ingestion + agent-scope work); this patch is mobile-side polish + remaining surface refinement that's been backlog-creeping since slice 8, plus the v0.3.0 carry-forward items.

**Source of truth carryover.** Items below were originally scoped in `build-phases/v0.1.1.md`; this is a literal lift, with status preserved. When an item ships, mark it ✅ here AND remove from `backlog.md` if it had a parallel entry. Items can move back to backlog freely if scope tightens.

**Relationship to v0.4.0 customization sprint.** v0.4.0 is the focused customization-framework + Dreams sprint (prompt decomposition, knobs, NL overlay, Todo specialist, Events end-to-end). v0.5.0 picks up what's left — UX polish + the v0.3.0 hangers that weren't cherry-picked into v0.4.0. The two are sequential by intent: v0.4.0 lands first, v0.5.0 follows once the foundational customization work is in place.

**Folded-in section structure.** The v0.3.0 hanger items live in a dedicated "Folded in from v0.3.0 (2026-05-19 sprint reset)" section near the bottom, with their original item numbers preserved + their original track grouping (A1/A2/B1/etc.) maintained for diff readability against v0.3.0.

**Legend**

- ✅ — done
- ✴️ — partial / in progress (status preserved from v0.1.1)
- ⏺️ — open / not started
- ⛔ — blocked

---

## In scope

### Promoted in from v0.2.0 cycle (out-of-original-scope)

0. ✅ **Todos sidecar refactor** — landed 2026-05-10. Substantial schema + UX rewrite, originally a P1 backlog item ("Project-scoped todos — architecture decision needed") that resolved with a generalized design and shipped immediately rather than slotting later. Summary:
    - **New `todos` sidecar table** (migration 0019) with `page_id` FK to wiki shell, `parent_page_id` nullable FK to ANY wiki page (project / goal / person / etc.), `status` enum, `due_date`, `completed_at`. RLS + realtime + RxDB collection wired.
    - **Status moved off the wiki hierarchy entirely.** The four `todos/<status>` bucket pages tombstoned + existing todos reparented as direct children of `todos` root (flat). Backfill derives initial sidecar status from old bucket slug.
    - **Two-axis Todos plugin UX:** horizontal status tabs (To do / In progress / Done / Archived) × vertical collapsible swimlanes by `parent_page_id` (General + per-page associations).
    - **Sidecar pattern proven** — first instance of the long-standing P0 architectural backlog item ("Sidecar tables for first-class wiki entities").
    - **Pro fan-out + Flash prompts** updated for the new flat structure + a new optional `todo_parent_slug` field on creates (default-NULL behavior; only set when transcript explicitly directs association).
    - **Live agent prompt** got a new "Todo associations — when to suggest a project / page" section guiding the agent to OFFER associations conservatively as confirmations during calls.
    - Shipped during the v0.2.0 cycle's late polish; placed in v0.4.0 because the visible UX win (swimlane plugin rewrite) is mobile-side and rides naturally with the rest of v0.4.0's UX pass.

### Visual surfaces — open polish from v0.1.1

1. ✴️ **Home screen update** — Lava lamp ambient ✅ (shipped in 0.1.1 globally; see `v0.1.1.md` item 1). Open: tile reorder, plugin icon iteration. Pixel-light, no dependencies.

2. ⏺️ **Tile-launch animation iteration** — The scale-from-tile transition into a plugin overlay landed in slice 7 but wants polish. Tighten the timing curves, ensure the source tile's position is captured precisely, and avoid the frame-1 white flash (resolved as a side effect of the LavaLamp + transparent contentStyle work, but the curve tuning still wants attention).

3. ⏺️ **Plugin content padding convention** — Every plugin overlay should reserve `safeAreaInsets.bottom` worth of padding at the bottom of its scroll content. The visual treatment is: content extends edge-to-edge horizontally and visually through the bottom edge of the device, but the *scroll area* always extends past the last content row by the safe-area amount, so the home indicator never overlaps the final row of content. Codify this as a wrapper component or a hook used by every plugin overlay's root list/scroll.

4. ✴️ **PluginOverlay base styling + layout iteration** — Translucent surface ✅ (`rgba(12, 19, 32, 0.8)` azure/80) + intensity-40 frost BlurView landed in 0.1.1; nested PluginStack `contentStyle: 'transparent'` patched so content area inherits the same translucency. Still ⏺️: header treatment (back row + title alignment), dismiss-handle visual, corner radii pass, and the safe-area content padding from item 3.

### Home + plugin grid

5. ⏺️ **Avatar tap → account / settings menu** (P1, S) — Top-right home avatar is a stub at MVP; this cycle surfaces the account / settings menu. Source: `specs/mobile-app.md`.

6. ⏺️ **Notification badges on home plugin tiles** (P1, S) — Per-tile "red dot" indicator (optionally with a count) showing new/unread activity for that plugin since last viewed. Sample triggers: Research = newly-completed research outputs you haven't opened; Todos = new agent-spawned todos in any non-archived bucket; Wiki = pages updated by ingestion since last visit. Implementation needs a per-tile "last viewed" timestamp (AsyncStorage initially; cloud-synced later) + reactive comparison against the live RxDB collections. Cleared when the user opens the overlay. Source: post-slice-8 UX request.

7. ⏺️ **Greeting subtext: live activity reflection** (P3, S) — Today the home greeting shows agent + plugin counts ("1 agent · 2 plugins"). Could show live activity ("1 research running · 5 todos pending") for a more dynamic surface. Pure polish. Source: post-slice-8 punt.

8. ⏺️ **Phone FAB ubiquity** (P0, S) — Available from every screen. Source: `specs/mobile-app.md` §19.

### Plugin overlays + navigation

9. ⏺️ **Gesture priority: plugin-dismiss vs system home-indicator** (P1, S) — When a plugin overlay is open (Wiki, Research, Profile, Todos), the iOS home-indicator swipe-up should **dismiss the overlay first**, not background the entire app. Only when the home screen itself is foregrounded (no overlay open, not in a call) should the system swipe-up gesture work normally. Implementation: `UIScreenEdgesDeferringSystemGestures` (iOS) — RN exposes via `useScreenEdgesDeferringSystemGestures` from `react-native-screens` or via Expo config. Enable on screens where we want swipe-up to be ours; disable on the home screen. Pairs naturally with the plugin-as-app navigation refactor — the deferring-gesture flag lives on the overlay shell. Android-side: `onBackPressed` already handles the back gesture, but pull-down-to-close is iOS-only so this is mostly an iOS concern. Source: post-slice-9 UX review.

10. ⏺️ **Pending-artifact placeholders in plugin overlays** (P0, M) — All artifact UIs (Research today; Podcast / Email-draft / Brief in V1+) should show in-flight artifacts as pending entries — not invisible-until-complete. User taps the Research tile mid-generation and sees "Researching: Italian restaurants in lower Manhattan… (~2 min)" with a spinner; row hydrates to the full output when ready. Big confidence win — proves the system is working without requiring users to wait blind. Generic pattern: any plugin overlay reads BOTH the artifact collection AND a "pending tasks" view (agent_tasks where status in ('pending','running') AND kind matches), unions them with kind-specific placeholder rendering. Requires syncing agent_tasks to mobile (currently server-only — would need RLS + realtime publication migration like research_outputs got). **Schema change:** the `agent_tasks` RLS/realtime migration is the only non-client change in this cycle's UX track; mirrors the `research_outputs` work in slice 6.5. Source: post-slice-7 UX feedback.

### Todos surface

11. ⏺️ **Todos: surface failed agent_task state on a row** (P1, S) — `useActiveAgentTasks` filters to `pending|running` only — failed research tasks become invisible. Extend the hook (or add a `useFailedAgentTasks`) and render a red error indicator + "tap to retry" affordance on the corresponding todo row. Wires into the existing `POST /calls/:id/retry-ingest` and a new `POST /tasks/:id/retry` (latter doesn't exist yet — small endpoint to add). Source: post-slice-8 punt.

12. ⏺️ **Failed-ingestion retry button (UI)** (P1, S) — Server endpoint already exists (`POST /calls/:id/retry-ingest`). Needs a UI trigger. Originally scoped to land on a call-history surface in slice 8; that surface didn't make it. Surface inline on the relevant row (todo / future call-history) rather than waiting on activity-stream UI. Source: slice 6.5 punt → slice 8 punt → v0.1.1 carryover.

13. ⏺️ **Todos: sub-tasks via hierarchy (rendering)** (P2, S) — Schema already supports nested todos (`parent_page_id` on a todo can point at another todo, not just a bucket). UI doesn't render hierarchy yet — todos appear flat under their bucket. Add indent-rendering when a todo's parent is another todo. Low urgency: agent-spawned todos always land directly under a bucket; this only matters for manual-create nesting. Source: post-slice-8 punt.

### Wiki section editor

14. ⏺️ **WYSIWYG editor choice + replacement of plain-markdown editor** (P0, M) — **Current state:** `WikiSectionEditor` is a bare RN `TextInput` with monospace fallback that holds raw markdown. Functional but unfriendly — users have to know markdown syntax, and the editor doesn't render bold/italic/lists/links inline as they type. **Target:** WYSIWYG section editor with the basic familiar affordances (bold, italic, lists, links, inline code, headings). Doesn't need to be feature-rich — just feel native + familiar. Candidates: Lexical (Meta's, RN-friendly), TipTap (ProseMirror-based, strong web-RN parity story), or a smaller RN-native editor library (`react-native-pell-rich-editor` is the lightest option, less polish). Storage layer stays markdown — only the input affordance changes. Source: `specs/mobile-app.md` §19 + post-slice-9 review. **Note:** this was flagged in v0.1.1 as the cycle's largest single item; it's the primary reason this phase exists separately.

### Call screen

15. ⏺️ **Mic-mute UI on call screen** (P2, S) — Distinct visual state for muted mic mid-call. Source: `specs/mobile-app.md` Call screen.

16. ⏺️ **Per-screen status-bar hiding** (P3, S) — Full-immersion mode for call screen. Source: `specs/mobile-app.md`.

---

## Out of scope

- **Agents homescreen tile / plugin** — lives in v0.2.0 (item #7) as the UX home for the open-questions queue; not duplicated here.
- **Theme switcher + light-mode toggle** — V1 work per v0.1.1's deferral; not promoted into this phase.
- **In-call transcript feed** — new surface, not polish.
- **Add-plugin tile / plugin marketplace surface** — tied to runtime plugin installation; feature-cycle work, not polish.
- **Wiki / CRUD / Todos / Projects / Graph / Activity-stream / Notifications / Call-history full UI modules** — each is a P0–P1 surface in its own right; this cycle refines what's already on the home grid.
- **Persona / agent config UX, in-call agent-config adjustment** — feature-y, not polish.
- **Conflict resolution policy / offline behavior / initial hydration** — sync infra, not UX.

---

## Demo target

A returning TestFlight user from v0.1.1 should immediately notice:

1. Editing a wiki section feels like editing rich text, not raw markdown.
2. A research task in flight shows up in the Research overlay with a spinner instead of vanishing until done.
3. A failed research task surfaces visibly on its todo row with a retry button.
4. Plugin overlays dismiss with a swipe-up instead of accidentally backgrounding the app.
5. The home grid feels "alive" — badges show new activity, the greeting reflects what's running.
6. Tapping the avatar opens settings.

If those six are true, the patch shipped.

---

## Open questions

- **Connectors track folded in or split out?** v0.2.0 deferred OpenClaw Path 1 connector work to "v0.3 or a parallel track." If we fold it in here, this phase grows considerably (new plugin family, OAuth flows, per-connector artifact tables, mobile UI per connector). If we split, connectors land as v0.3.1 or a parallel track. Decide before sequencing.
- **Sequencing relative to v0.2.0.** Both phases are independent; either can ship first, or they can run in parallel (different work, different files, minimal merge surface). Probably worth picking one to anchor on so the dogfood loop has a clear cadence.
- **WYSIWYG editor library choice.** Lexical vs TipTap vs `react-native-pell-rich-editor` is a discrete decision that shapes the cycle's effort. Worth a focused evaluation before scheduling — wrong choice doubles the cost.
- **Versioning.** Marketing version `0.2.1` in `app.json` before first preview build. Stick on `preview` channel; promote to `production` only after dogfood.

---

## Folded in from v0.3.0 (2026-05-19 sprint reset)

These items did not ship in v0.3.0 and were not cherry-picked into the v0.4.0 customization sprint. They carry forward here with status preserved + their original v0.3.0 item numbers + track groupings (for diff readability against v0.3.0). For full design rationale on any item, see the corresponding entry in `build-phases/v0.3.0.md`.

### A1 — Live Agent

- **#1** ⏺️ Read tools — `search_research`, `fetch_research`, `search_storage`, `fetch_storage`. Live agent pulls recent artifacts on demand; per-tool server endpoint under user JWT (RLS-scoped); usage_event per call; tool palette registration.
- **#2** ⏺️ Pending-capture UI indicator on call screen — chip that acknowledges intents ("Audri noted: 1 item will land after this call") via a `note_pending(summary)` UI-only tool call (no DB writes).
- **#3 (UX-coaching slice only)** ⏺️ Live Agent tour-guide / coaching capability — the agent walks users through features when asked "how do I do X?". *App Map / prompt-side awareness slice was cherry-picked into v0.4.0 as part of plugin-registry-as-App-Map.*
- **#4** ⏺️ Live abort tool — mid-action cancellation when user pivots ("never mind, let's talk about X").
- **#5** ⏺️ Agent-driven call-end tool `end_call(reason)` — closes the awkward dead-air after pleasantries. Mirrors `POST /calls/:id/end` with `end_reason='agent_ended'`.
- **#6** ⏺️ Proactive call-end / adaptive guardrail — short cap (~2 min) default with extension when conversation signals long-form work; depends on #5.
- **#70** ⏺️ Text-chat mode — parallel I/O channel to live call; same agent foundation, different plumbing. Composer prunes voice-specific scaffolding when `mode='text'`.
- **#71** ⏺️ Incognito calls — `call_type='incognito'` opts out of post-call ingestion. Distinct preload + prompt clause + UI treatment.
- **#72** ⏺️ Mic auto-pause on silence — 30s default; token-saver for thinking pauses. Tunable via #74 mic-sensitivity setting.
- **#100 (preload-side slice only)** ⏺️ Widen Live Agent preload (recency-weighted + project-graph proximity?) so common-case pages don't need to be searched. *Prompt-side "when/how to search" cues were cherry-picked into v0.4.0 prompt decomp.*

### A2 — Ingestion quality

- **#10** ⏺️ Prefilter: decouple page-type from hierarchy routing. Type stays a semantic label; routing decided independently.
- **#14** ⏺️ Voice-driven wiki mutations cluster — tombstone + rename. `tombstoned: true` + `title` on `PageUpdate` (slug stays stable as identity). Only act on explicit directives.
- **#15** ✴️ Reflection pass on the Pro fan-out prompt — ongoing iteration umbrella. Today's concrete fixes landed in v0.3.0 (synthetic example, agent_abstract narrowing, schema-level KEEP-AS-IS removal, agent_notes capture, no-op guard broadening). Item stays in-flight for future iteration; new active bug-fix work flows through v0.4.0 prompt decomp.

### A3 — Research handler quality

- **#16** ❓ Research modes (Discovery / Comparison / Verification / Recommendation) — decision deferred until research-handler refinement work. Evaluate against real research output samples whether mode split adds value.
- **#17** ⏺️ 5W rubric continued iteration. No new code unless field test reveals over/under-correction.
- **#18** ⏺️ Empty-response diagnostic — read v0.2.1's captured Sentry events; adjust safety-trigger handling or retry policy based on patterns.

### A4 — Pipeline resilience

- **#78** ⏺️ Switch research handler to `generateContentStream`. Follow-up to v0.3.0 #67 (revert), but research uses interactive tool-calling so streaming adds complexity around mid-stream function-call chunks. Fire if research starts hitting transport-side timeouts.
- **#79** ⏺️ Chunked ingestion for large documents — split docs over ~50k tokens into chapter-sized chunks, per-chunk fan-out, synthesize into a single source page. Pairs with prompt caching.

### B1 — Automations primitive

- **#23** ⏺️ Reminders — recurring `agent_tasks` touching todos sidecar. Eager rolling todo creation. NL extraction parses "remind me to X" with structured `{ title, dueAt | dueDate, allDay, recurrence | null }`.
- **#24** ⏺️ Daily Recap — first app-level automation. `scope='app'` registry flag; output to `automations/daily-recap/YYYY-MM-DD` under lazy-created `automations/` root.
- **#25** ✴️ Dreaming = automation of Agents — substrate shipped 2026-05-13 (recurring infra, trigger_mode, dreaming enum, registry entry, on-call-end hook); handler implementation tracked under v0.4.0 Track E (Dreams D1–D7). Item stays in-flight here as the Automations-side tracker.
- **#26** ⏺️ `kind='dream'` enum addition — small migration. Promotion mutates kind alongside parent_page_id.
- **#79b (Healthcheck)** ⏺️ Periodic wiki-integrity sweep — contradictions, stale claims, orphan pages, missing cross-references, knowledge gaps. Output to `/automations/healthcheck/<fire-stamp>`. New `agent_task_kind='healthcheck'`; weekly suggested default off. *(Numbered #79 in v0.3.0 B1 — distinct from A4 #79 chunked-ingestion entry.)*

### B2 — Storage / upload pipeline

- **#27** ✴️ Upload pipeline — backend shipped 2026-05-14 (commit 4df3f5a): `audri_storage` bucket, `uploads` + `upload_attachments` + `wiki_section_uploads` tables, server endpoints, per-kind extractors, worker tasks, doc-aware prompts. **Open:** mobile UX (StorageOverlay locally written, uncommitted) + prompt dogfood on real PDFs/markdown/research papers.
- **#80** ✴️ URL-as-source pipeline — backend shipped 2026-05-14 (commit 27d9c90): `url_sources` + junctions, `web_article` / `pdf` / `reddit_thread` kinds, extractors, worker tasks, dedicated prompts. **Open:** mobile UX, prompt dogfood.
- **#69** ⏺️ Share Target (iOS Share Extension, drop-only) — system share-sheet → Storage. Inline attach-picker deferred further.

### B3 — Archive

- **#28** ✴️ `archived_at` substrate shipped 2026-05-13 (migration 0027, all read sites filtered). **Open:** `POST /me/pages/:slug/archive` + `/unarchive` endpoints with descendant-cascade helper; archive/unarchive UI affordance; archived-pages browse view in Notes.

### B5 — Tags

- **#30** ⏺️ Activate `tags` + `wiki_page_tags` — schema shipped, zero rows. Pro emits suggested tags on PageCreate/PageUpdate; user-applied via Notes UI; source-relationship tags auto-applied (`#has-research`, `#discussed-recently`).

### B7 — Cross-cutting plugin substrate

- **#74** ⏺️ Microphone sensitivity — app-level config; new knob in home-screen Settings-Cog drawer; controls barge-in + auto-pause silence thresholds.
- **#75** ⏺️ Badge notifications on home plugin tiles — per-tile red dot (+count) when new content delivered. Per-tile "last viewed" timestamp + reactive comparison against RxDB collections.

### C1 — Notes plugin (leftover after cherry-picks)

*(#32 wiki cross-linking was cherry-picked to v0.4.0.)*

- **#33** ⏺️ Add-page affordance in Notes UI — explicit "+ New page" button; parent picker defaults to current page. ⚠️ Architectural gotcha: rxdb-supabase client-INSERT fails on push-enabled tables; route through server endpoint.
- **#34** ⏺️ Strict page ordering — `sort_order int` on `wiki_pages`; drag-to-reorder. Open at design how voice ops + ingestion interact with order.
- **#35** ⏺️ Filesystem-style left drawer (Notes + Storage) — reusable drawer pattern; pinned + page tree + recents.
- **#36** ⏺️ Tags surface in Notes plugin — tag-chip rendering on page detail; filter-by-tag; tag-driven views. Depends on #30.
- **#37** ⏺️ Unique icons for default profile-page sections.

### C2 — Todos plugin (leftover after cherry-picks)

*(#38 sub-tasks rendering was cherry-picked to v0.4.0 Todo specialist work.)*

- **#39** ⏺️ Surface failed `agent_task` state on a todo row — extend `useActiveAgentTasks` to render red error indicator + "tap to retry"; new `POST /tasks/:id/retry` endpoint.
- **#40** ⏺️ Project-scoped todo lists rendering — surface project-scoped views in Todos overlay (already in data model).

### C3 — Automations plugin

- **#41** ✴️ Automations tile + navigation — shipped 2026-05-13 (commit 82568ec), end-to-end dogfood pending.
- **#42** ✴️ Automation create / edit / pause / delete UX — shipped 2026-05-13, dogfood pending.
- **#43** ✴️ Per-plugin suggested-defaults seeding — shipped 2026-05-13, dogfood pending.

### C4 — Storage plugin mobile UX

- **#44** ✴️ Storage plugin tile + overlay — mobile UX in flight (uncommitted).
- **#45** ✴️ Upload affordance — `AddFileScreen` + `AddUrlScreen` → `AttachScreen` flow.
- **#46** ✴️ Source attachment to wiki pages — `AttachScreen` creates per-attachment rows; inverse "Sources" panel on wiki-page detail pending.

### C5 — Research plugin

- **#47** ⏺️ Recent-artifacts-in-call surface — preload includes recent N research outputs (title + kind + 1-line summary + timestamp). Pairs with A1 #1 read tools.
- **#48** ⏺️ Research detail view refinement — source citations, follow-up affordances, "spawn related research".
- **#49** ⏺️ Research → wiki reingestion UX — flips `reingestsIntoWiki=true`; ingestion treats as transcript-like input writing wiki sections cited back.

### C6 — Agents plugin

*(#50–58 were cherry-picked to v0.4.0 — Agents tile is the home for Dreams review surface + potentially the Scribe configuration surface per spec Open Question A.)*

### C7 — Onboarding

*(Onboarding is explicitly deferred to the END of v0.4.0 per the customization-framework spec; the items below stay parked here until v0.4.0 close.)*

- **#59** ⏺️ Onboarding completion criteria + deferral mechanism — replace binary `onboarding_complete` with `pending|deferred|complete` enum + criteria.
- **#60** ⏺️ Onboarding modes (long vs short) — split based on user context; adds 3rd phase (Self-Introduction → Expectation-Setting → User Interview).
- **#61** ⏺️ Onboarding checklist — persistent setup nudge card; tracks setup milestones.
- **#62** ⏺️ Onboarding script: more explicit about capabilities + UX flow.
- **#63** ⏺️ Onboarding script: dreaming line restored.

### C8 — Account

- **#68** ⏺️ Account > Usage: time-period breakdowns — Today / 7 days / This month / YTD selector.
- **#99** ⏺️ Account > Usage: split spend by surface (Live Agent vs Notes vs Other).

### C9 — Call screen

- **#77** ⏺️ Call screen polish — cohesive design pass pulling pending-capture chip, mic-pause state, incognito indicator, end-call states, read-tool activity indicators, error states.

### D — Interconnection

- **#64** ⏺️ Cross-link cluster: chats ↔ notes ↔ artifacts — chat detail's "Notes touched" + Artifacts produced + inverse "discussed in" / "produced from" panels.
- **#65** ⏺️ Artifact → first-class entity routing — reingest as source, attach to wiki page, convert to another artifact kind.
- **#66** ⏺️ Uploads-as-sources to wiki pages — covered by #46 + inverse rendering; framed under interconnection.

### E — Dreams Deep tier (D8 only)

*(D1–D7 were promoted to v0.4.0 per the customization-framework spec.)*

- **#97 (D8)** ⏺️ Deep weekly synthesis — whole-KB pass; weekly cadence; highest cost. Added once REM behavior is understood in practice.

### Dogfood backlog (rolled up from v0.3.0)

Five items shipped in v0.3.0 without hands-on validation; bundle into one pass when convenient (see `project_v030_dogfood_pending` memory):

1. Schedule timezone fix verify (`utcFromLocal` single-step compensation).
2. C3 Automations tile end-to-end (#41–43).
3. `brief_me` + `stalled_work` handler prompts — read real-data output for tone.
4. Upload pipeline prompts (#27) — PDF / markdown / research paper extraction + fan-out routing.
5. URL-source pipeline prompts (#80) — web article / reddit thread / hosted PDF extraction + fan-out routing.

Plus the in-flight mobile UX (#44–46) needs to land + smoke-test before either pipeline can be exercised end-to-end from the phone.

**UX rename pending — "Storage" → "Uploads"** (noted 2026-05-15 during dogfood). User-facing tile is labeled "Storage" but mental model is "Uploads". Rename across mobile UX surfaces only; internal identifiers (`audri_storage` bucket, `uploads` table) stay.
