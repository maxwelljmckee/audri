# Build plan — v0.1.1 (UX polish + prompt engineering)

A patch-version cycle between MVP (0.1.0, shipped 2026-04-29) and the next feature wave. **Two parallel work streams:** (a) mobile-side UX polish — the rough edges that became visible once the app was usable end-to-end on TestFlight; (b) prompt-engineering tranche — addresses gaps revealed by the first real-world test calls (see "Prompt engineering tranche" section below). Both ship together as 0.1.1.

The UX cycle splits into two passes: a **visual-identity + core surfaces** pass (the priority items below — splash, auth, onboarding, home, call, plugin shell), and a **rough-edges** pass (the smaller backlog-derived items pulled forward into this patch).

Items below are copied from `backlog.md` so this doc is self-contained for the cycle. When an item ships, mark it ✅ here AND remove (or strikethrough + mark "shipped in 0.1.1") in the backlog. If we change our minds on scope mid-cycle, items can move back to `backlog.md` freely.

**Legend**

- ✅ — done
- ✴️ — partial / in progress
- ⏺️ — open / not started
- ⛔ — blocked

---

## In scope

### Priority — visual identity + core surfaces

The top-priority block. These items reshape the look + feel of the app end-to-end and should land first; the rough-edges items below build on the new shell.

1. ✅ **Full-application backdrop layer** — Replaced the original black-token backdrop with a global `<LavaLamp>` (Animate React Native, project-owned) at the Expo Router root: 5 in-family blue blobs on a desaturated azure surface (`#0c1320`-class) under a `BlurView intensity=100, tint='dark'` frost. Custom placement math keeps blob centers ~on-screen with a 20% overshoot. Stack `contentStyle.backgroundColor: 'transparent'` everywhere so transitions don't paint over it. Pause-on-overlay wired (see tradeoffs.md). Shipped 2026-05-04.

2. ✅ **Splash screen + app-launch animation** — JS-side `SplashAnimation` overlay choreographs: 200ms pre-roll (silent) → 800ms hold with paired fade-in + scale (1 → 1.08) + lift (0 → -4px) → 150ms collapse of the `udr` clipping container → 500ms settle → 300ms overlay fade-out. Native splash uses solid azure (no logo PNG) for seamless handoff. Plays once per cold-start via a zustand singleton. Shipped 2026-05-01 (iterated through 2026-05-02).
   - 2b. ✅ **Iterate on app logo font** — Locked **Comfortaa** (regular, 400) as the wordmark across auth + home + animation. Replaced Sniglet. Decision context: evaluated Quicksand, Comfortaa, Mulish, Nunito, Be Vietnam Pro, Lexend on a debug route + on the live auth/home screens via a shared font-toggle. Final shortlist Comfortaa vs Nunito; Comfortaa won on the standalone `Audri` resting state. Shipped 2026-05-01.

3. ✅ **Auth screen redesign** — Stripped the "Sign in to continue" subtitle and the wide "Continue with Google" button. Replaced with a row of 3 square `GlassButton` provider tiles (Google active; Apple + Facebook stubbed for staged backend wire-up). Comfortaa wordmark on transparent bg over the LavaLamp. Email button removed. Shipped 2026-05-02.

4. ✅ **Onboarding screen redesign** — Minimal landing: skip-link top-right, CallButton at bottom-center (pixel-pinned to home's FAB position so the post-tap transition is spatially continuous). Three guidance lines stagger-fade in on a 3s cadence (`Turn up phone volume` / `and find a quiet place` / `to take your onboarding call`), then `Tap to Start` follows. Live phase uses `<CallButton mode="end">`. Shipped 2026-05-04.

5. ✴️ **Home screen update** — Lava lamp ambient ✅ (global, not just home). Tile reorder ⏺️ open. Plugin icon iteration ⏺️ open. The two open sub-items are pixel-light and don't depend on anything else.

6. ✅ **Reusable base components** — All three shipped, plus extra polish landed along the way:
   - **`GlassButton`** ✅ — `expo-glass-effect` GlassView on iOS 26+, `BlurView` fallback elsewhere. Defensive `try/catch` around the capability check so missing native module fails to BlurView path instead of crashing. White hairline border + 4% white surface wash on the fallback path so tiles read clearly on dark bg. Tint-aware: when a `tintColor` is provided, fallback paints a colored overlay (`${tintColor}99`) on top of the BlurView so per-button branding survives the non-glass path.
   - **`CallButton`** ✅ — 80px circle, `mode='start'` → emerald-500 / `mode='end'` → rose-500 (with rotated phone icon).
   - **`TileButton`** ✅ — square aspect-ratio glass tile (used internally; `PluginTile` shim retains the origin-rect capture for the launch animation).
   - **Cross-cutting**: `Haptics.impactAsync` (Light on tap, Medium on long-press) wired into `GlassButton` once and inherited by all consumers. Both gated on the consumer actually passing the corresponding handler so untouchable buttons don't buzz.

7. ⏺️ **Tile-launch animation iteration** — The scale-from-tile transition into a plugin overlay landed in slice 7 but wants polish. Tighten the timing curves, ensure the source tile's position is captured precisely, and avoid the frame-1 white flash (resolved as a side effect of the LavaLamp + transparent contentStyle work, but the curve tuning still wants attention).

8. ⏺️ **Plugin content padding convention** — Every plugin overlay should reserve `safeAreaInsets.bottom` worth of padding at the bottom of its scroll content. The visual treatment is: content extends edge-to-edge horizontally and visually through the bottom edge of the device, but the *scroll area* always extends past the last content row by the safe-area amount, so the home indicator never overlaps the final row of content. Codify this as a wrapper component or a hook used by every plugin overlay's root list/scroll.

9. ⏺️ **Call screen — Orb Shader animation** — Replace the current Orb component (`components/Orb.tsx`) with a shader-driven animation. Should respond to call state: idle (calm) → user speaking (subtle pulse) → agent speaking (more active) → interrupted/error (distinct visual). React-native-skia or a Reanimated-driven canvas are likely candidates; pick at design time. Pairs with the new backdrop layer — the orb floats over it.

10. ✴️ **PluginOverlay base styling + layout iteration** — Translucent surface ✅ (`rgba(12, 19, 32, 0.8)` azure/80) + intensity-40 frost BlurView landed; nested PluginStack `contentStyle: 'transparent'` patched so content area inherits the same translucency. Still ⏺️: header treatment (back row + title alignment), dismiss-handle visual, corner radii pass, and the safe-area content padding from item 8.

---

### Home + plugin grid

- ⏺️ **Avatar tap → account / settings menu** (P1, S) — Top-right home avatar is a stub at MVP; v0.1.1 surfaces the account / settings menu. Source: `specs/mobile-app.md`.
- ⏺️ **Notification badges on home plugin tiles** (P1, S) — Per-tile "red dot" indicator (optionally with a count) showing new/unread activity for that plugin since last viewed. Sample triggers: Research = newly-completed research outputs you haven't opened; Todos = new agent-spawned todos in any non-archived bucket; Wiki = pages updated by ingestion since last visit. Implementation needs a per-tile "last viewed" timestamp (AsyncStorage initially; cloud-synced later) + reactive comparison against the live RxDB collections. Cleared when the user opens the overlay. Source: post-slice-8 UX request.
- ⏺️ **Greeting subtext: live activity reflection** (P3, S) — Today the home greeting shows agent + plugin counts ("1 agent · 2 plugins"). Could show live activity ("1 research running · 5 todos pending") for a more dynamic surface. Pure polish. Source: post-slice-8 punt.
- ⏺️ **Phone FAB ubiquity** (P0, S) — Available from every screen. Source: §19.

### Plugin overlays + navigation

- ⏺️ **Gesture priority: plugin-dismiss vs system home-indicator** (P1, S) — When a plugin overlay is open (Wiki, Research, Profile, Todos), the iOS home-indicator swipe-up should **dismiss the overlay first**, not background the entire app. Only when the home screen itself is foregrounded (no overlay open, not in a call) should the system swipe-up gesture work normally. Implementation: `UIScreenEdgesDeferringSystemGestures` (iOS) — RN exposes via `useScreenEdgesDeferringSystemGestures` from `react-native-screens` or via Expo config. Enable on screens where we want swipe-up to be ours; disable on the home screen. Pairs naturally with the plugin-as-app navigation refactor — the deferring-gesture flag lives on the overlay shell. Android-side: `onBackPressed` already handles the back gesture, but pull-down-to-close is iOS-only so this is mostly an iOS concern. Source: post-slice-9 UX review.
- ⏺️ **Pending-artifact placeholders in plugin overlays** (P0, M) — All artifact UIs (Research today; Podcast / Email-draft / Brief in V1+) should show in-flight artifacts as pending entries — not invisible-until-complete. User taps the Research tile mid-generation and sees "Researching: Italian restaurants in lower Manhattan… (~2 min)" with a spinner; row hydrates to the full output when ready. Big confidence win — proves the system is working without requiring users to wait blind. Generic pattern: any plugin overlay reads BOTH the artifact collection AND a "pending tasks" view (agent_tasks where status in ('pending','running') AND kind matches), unions them with kind-specific placeholder rendering. Requires syncing agent_tasks to mobile (currently server-only — would need RLS + realtime publication migration like research_outputs got). Source: post-slice-7 UX feedback. **Note:** the agent_tasks RLS/realtime migration is the only schema change in this cycle; everything else is client-only.

### Todos surface

- ⏺️ **Todos: surface failed agent_task state on a row** (P1, S) — `useActiveAgentTasks` filters to `pending|running` only — failed research tasks become invisible. Extend the hook (or add a `useFailedAgentTasks`) and render a red error indicator + "tap to retry" affordance on the corresponding todo row. Wires into the existing `POST /calls/:id/retry-ingest` and a new `POST /tasks/:id/retry` (latter doesn't exist yet — small endpoint to add). Source: post-slice-8 punt.
- ⏺️ **Failed-ingestion retry button (UI)** (P1, S) — Server endpoint already exists (`POST /calls/:id/retry-ingest`). Needs a UI trigger. Originally scoped to land on a call-history surface in slice 8; that surface didn't make it. For v0.1.1, surface inline on the relevant row (todo / future call-history) rather than waiting on activity-stream UI. Source: slice 6.5 punt → slice 8 punt.
- ⏺️ **Todos: sub-tasks via hierarchy (rendering)** (P2, S) — Schema already supports nested todos (`parent_page_id` on a todo can point at another todo, not just a bucket). UI doesn't render hierarchy yet — todos appear flat under their bucket. Add indent-rendering when a todo's parent is another todo. Low urgency: agent-spawned todos always land directly under a bucket; this only matters for manual-create nesting. Source: post-slice-8 punt.

### Wiki section editor

- ⏺️ **WYSIWYG editor choice + replacement of plain-markdown editor** (P0, M) — **Current state (post-MVP):** `WikiSectionEditor` is a bare RN `TextInput` with monospace fallback that holds raw markdown. Functional but unfriendly — users have to know markdown syntax, and the editor doesn't render bold/italic/lists/links inline as they type. **v0.1.1 target:** WYSIWYG section editor with the basic familiar affordances (bold, italic, lists, links, inline code, headings). Doesn't need to be feature-rich — just feel native + familiar. Candidates: Lexical (Meta's, RN-friendly), TipTap (ProseMirror-based, strong web-RN parity story), or a smaller RN-native editor library (`react-native-pell-rich-editor` is the lightest option, less polish). Storage layer stays markdown — only the input affordance changes. Source: §19 + post-slice-9 review. **Note:** this is the largest item in the cycle (M effort); if it ends up dominating the patch's scope, consider promoting to its own 0.1.x slot and shipping the rest of v0.1.1 first.

### Call screen

- ⏺️ **Mic-mute UI on call screen** (P2, S) — Distinct visual state for muted mic mid-call. Source: `specs/mobile-app.md` Call screen.
- ⏺️ **Per-screen status-bar hiding** (P3, S) — Full-immersion mode for call screen. Source: `specs/mobile-app.md`.

---

## Prompt engineering tranche

Pulled forward from `backlog.md → Prompt engineering (near-term, high-priority)`. The first real-world test calls on 2026-05-01/02 surfaced concrete gaps these items address: Pro fan-out commits zero sections on perfectly-substantive transcripts because the prompt is too conservative about creating sections, and there's no claim-level audit trail to debug *why* each decision was made.

**Architectural note (turn-level resolved 2026-05-05):** the backlog flagged "consider conditional prompt routing before scoping this tranche." The **turn-level case** is now resolved by `specs/conversational-routing.md` — static prompt at `/calls/start` with the model self-routing per turn, no dynamic prompt re-composition during the call. The **call-level case** (does prompt structure differ across call types — onboarding vs generic vs future incognito etc.) is still open and lives in the backlog; it overlaps less with this tranche than originally feared.

### Onboarding scaffolding

- ✴️ **Core UX Principles in onboarding scaffolding** — Code shipped 2026-05-06, **field-test pending**. Mirror of the Conversational Routing Phase 1 principles fold-in, recontextualized for onboarding's agent-led structure: `apps/server/src/calls/system-prompt.ts` → `composeOnboardingScaffolding` got a new `# Principles` section appended after `# Progress + wrap`. Four bullets — Proactiveness ("onboarding IS proactiveness; but don't barrel through agenda at user's cost"), Transparency ("tell them what you're capturing as you go; user should trust the wiki because it was built openly"), Continuity ("first call so most continuity refs are forward-looking — seed the cumulative-relationship posture"), Autonomy ("most load-bearing here — stop when they stop, skip what they skip, follow when they redirect"). Source: pre-dogfood end-to-end UX coherence pass 2026-05-06.

### Live agent prompt

- ✴️ **Conversational Routing — Phase 1 (scaffolding)** — Code shipped 2026-05-06, **field-test pending**. Per-turn intent → agent-persona modulation, gated by information provenance (User / World / Conversation as source), gated to user-led call types only (`generic`). Spec at `specs/conversational-routing.md`. Subsumes the original "Conversational modes (Brainstorm + Dictation)" backlog item AND the "Explicit UX core principles in scaffolding" item — the four core principles (Proactiveness / Transparency / Continuity / Autonomy) are folded as the foundational layer underneath the persona system. Shipped: prose added to `composeGenericScaffolding` in `apps/server/src/calls/system-prompt.ts` — four behaviorally-described postures (Self-exploration / Information-seeking / Brainstorming / Capture; persona names stay spec-side reference only), provenance lens, momentum prose for inertia, primary-thread prose for ambiguity, four core principles. Persona names (Active Listener, Thought Partner, etc.) intentionally NOT in the prompt — sidesteps the failure mode where the model voices internal vocabulary. Onboarding scaffolding untouched (agent-led). Phase 2 (telemetry) backlogged — see below + `backlog.md`. **TODO before marking ✅:** dogfood a generic call covering all four postures (one self-exploration prompt, one info question, one brainstorm, one "just remember this") and read the transcript — verify response style plausibly shifts per turn. Source: post-MVP review 2026-05-01 + 2026-05-05 framework + 2026-05-06 implementation pass.
- ⏺️ **Conversational Routing — Phase 2 (telemetry)** — Backlogged. Implementation pass surfaced that audio-modality output means an inline intent tag would be vocalized, and tool-call infrastructure for an alternative `set_turn_intent` tool isn't wired yet. Two Phase 2 shapes (tool-call when general infra lands; post-hoc Flash classification at ingestion). Tracked in `backlog.md` → "Conversational Routing — Phase 2 telemetry."

### Ingestion prefilter (Flash candidate retrieval)

- ✴️ **Voice-driven hierarchy moves on existing pages** — Code shipped 2026-05-06, **field-test pending**. Promoted from backlog mid-cycle when surfaced as a known gap (the user's hypothetical: "please move social-technology and interdependence under the Consensus project" — pre-fix, Pro had no operation to express the move). Changes: (a) `PageUpdate` interface gains `parent_slug?: string | null` (three-state: omitted = no change, null = top-level, string = move under that slug) AND `sections` becomes optional (omit for move-only updates so existing sections aren't tombstoned); (b) Pro responseSchema reflects both — `parent_slug` nullable, `sections` removed from `required`; (c) Pro prompt: new "Hierarchy moves on existing pages" sub-rule under §3 routing (only act on EXPLICIT user directives — "move X under Y", "make X top-level" — never infer); (d) empty-update-suppression rule got an explicit carve-out for move-only updates ("the move IS the meaningful change"); (e) hard-rules + JSON-shape annotation updated to reflect the optional sections contract with the explicit "never emit `sections: []` for move-only" warning; (f) `commit.ts` reads `parent_slug` from updates, resolves it (same lookup pattern as creates), updates `parentPageId`. Section processing + tombstone-not-in-list logic now gated on `update.sections !== undefined` so move-only updates don't nuke page content. (g) Flash got a new "Move patterns → ALWAYS flag both source and target" rule mirroring the existing "Commitment patterns → flag todos/todo" pattern, ensuring both ends of a move are in Pro's candidate set; (h) `specs/fan-out-prompt.md` and `specs/flash-retrieval-prompt.md` mirrored. Tombstone + rename via voice deferred to V1+ (see backlog → "Voice-driven wiki mutations cluster"). **TODO before marking ✅:** dogfood with a transcript that says "move A and B under C" where A/B/C all exist; verify (i) Flash flags A, B, C; (ii) Pro emits two updates with parent_slug=C and no sections field; (iii) commit step changes A and B's parent_page_id to C's id without tombstoning their existing sections. Source: post-MVP design discussion 2026-05-06.

- ✴️ **New-page suggestion: better hierarchy logic** — Code shipped 2026-05-06, **field-test pending**. Flash now emits `proposed_parent_slug` on every new_pages entry. Changes: (a) `NewPage` interface gains `proposed_parent_slug: string | null`; (b) Flash's `responseSchema` adds the field as required (nullable); (c) Flash's system prompt got a new "Proposing parent_slug — top-level is RARE" section with the same priority order shared with Pro (people→`profile/relationships`, orgs→`profile/work`, standalone concepts→`profile/interests`, etc.) plus the type-bucket allow-list + top-level-is-rare bias mirroring Pro; (d) Flash's output-contract hard rules updated; (e) Flash's existing "Profile sub-pages → propose on-demand" section's example output updated to include `proposed_parent_slug: "profile"`; (f) two new Flash worked examples added — Example 4 (sub-concepts under existing project, parent=project's slug) and Example 5 (new person, parent=`profile/relationships`); (g) Pro prompt routing rule 3 updated: was "may override the proposed type"; now "may silently override the proposed type AND/OR the proposed_parent_slug" with explicit guidance that Pro defaults to Flash's hint and overrides only with content-grounded reason; (h) Pro's input-contract description updated to reflect the new field; (i) `specs/flash-retrieval-prompt.md` and `specs/fan-out-prompt.md` mirrored. Tradeoff doc entry "Pro silently overrides Flash's `proposed_parent_slug`" captures the design rationale. Source: post-MVP review 2026-05-01 + 2026-05-06 implementation pass.
- ✅ **Refactor handling of page types** — Shipped via Bundle 1 (2026-05-06). Promoted from P1 to P0 mid-cycle when the 2026-05-06 incident made it concrete; type/hierarchy decoupling is now load-bearing across Pro + Flash + Live Agent prompts via the type-bucket allow-list (`profile`, `todos`, `projects`) + the top-level-is-rare refinement. See Bundle 1 entry below for the full layer-by-layer diff.

### Ingestion Pro fan-out prompt

- ✴️ **Worked nesting example + top-level-is-rare refinement** — Code shipped 2026-05-06, **field-test pending**. Added a "Wiki shape — worked example" section to `apps/worker/src/ingestion/pro-fan-out.ts` SYSTEM_PROMPT, then immediately tightened the structural rule based on user feedback: the bar for top-level pages must be HIGH. Bundle 1 originally treated `parent_slug: null` as an acceptable fallback ("top-level entity pages are FINE"); the refined rule inverts this — top-level is RARE, only legitimate when the transcript explicitly directs top-level treatment. Almost everything nests under `profile/<area>`. **Refinements landed:** (a) Pro routing rule 3 rewrote the priority order — people default to `profile/relationships`, orgs to `profile/work` or non-canonical `profile/communities`, standalone concepts to `profile/interests`; orphan content picks the closest profile-area parent rather than null; (b) Pro hard-rules bullet on `parent_slug` strengthened from "SHOULD be set" to "REQUIRED in nearly all cases — null ONLY with explicit user direction"; (c) the worked example tree was revised — `sarah-chen` and `alex-rivera` now nest under `profile/relationships`, `anthropic` under `profile/work`, `information-theory` under `profile/interests`; the "top-level entity pages — no parent" cluster removed and replaced with "[NO top-level entity pages in this example — see the rule below]"; (d) Live Agent prompt's wiki-structure section gained the same priority-order heuristics + a refined ask-when-ambiguous rule that biases suggestions toward profile-area homes (don't lead with "make it top-level"); (e) `specs/fan-out-prompt.md` mirrored. Hypothetical user "Maya" + Consensus project structure preserved as the concrete tree. Pro-prompt-only for v1; will mirror to Live Agent only if field-testing shows the structural rules alone aren't enough. Source: post-MVP discussion 2026-05-06 + refinement same day.
- ✴️ **Favor page hierarchy when inserting new pages + sections** (P0, S) — Code shipped 2026-05-06 in two passes (subsumes the original "Section creation on updates" + the 2026-05-06 type-bucket fix; bundled together as Bundle 1 below). Field-test pending. Pro prompt now: (a) requires `parent_slug` on creates with semantic heuristics; (b) creates new sections rather than skipping when the target page has no fitting section (addresses 2026-05-02 incident); (c) enforces a strict type-bucket allow-list (`profile`, `todos`, `projects` only) — never invent `concepts`/`places`/`people`/`events` parents (addresses 2026-05-06 incident); (d) respects explicit user direction from the transcript over its own heuristics. Mirrored in `specs/fan-out-prompt.md` §4.3.
- ✅ **Reflection pass on `specs/fan-out-prompt.md`** — Done 2026-05-06 inline with the hierarchy work. Structural review confirmed the targeted fixes are the right ones. Caveat: structural review only — no local DB read path for production transcripts. Future iterations can dig deeper if real misroutes surface.

#### Bundle 1 — wiki structure is user-driven, not model-inferred (2026-05-06)

A coherent four-layer fix triggered by the 2026-05-06 incident (sub-topics of "Consensus" filed under an invented `Concepts` top-level bucket instead of nesting under their parent project). Promoted from item 4 ("Refactor handling of page types") with priority bumped P1→P0 because the user flagged this as blocking MVP-functional status. All four layers shipped together:

- ✴️ **Layer 1 — Seed `projects` bucket + universal sub-page trim (profile + agent-scope).** Added `PROJECT_PAGES` (flat root only) to `apps/server/src/seed/seed.constants.ts`. Trimmed PROFILE_PAGES from 10 (root + 9) to **just the root** (1), AND trimmed AGENT_SCOPE_PAGES from 5 (root + 4) to **just the root** (1). Every `profile/<area>` and `assistant/<area>` sub-page is now created on-demand by ingestion when content matches. The principle: empty seeded sub-pages aren't load-bearing structure; they're noise that pollutes the structural-snapshot preload AND gives the model false impressions of what the user (or assistant) has actually accumulated. Total seeded now **8 wiki_pages**: 1 agent-scope root + 1 profile root + 5 todos (root + 4 status buckets, retained because the status workflow is functional structure not empty containers) + 1 projects root. **Profile vocabulary** (lives in Flash + Pro + Live Agent prompts): the 7 askable areas (`profile/goals`, `/life-history`, `/health`, `/work`, `/interests`, `/relationships`, `/preferences`) plus the 2 emergent (`profile/values`, `/psychology`); non-canonical sub-pages (e.g. `profile/finances`, `/spirituality`) when content warrants. **Agent-scope vocabulary** (lives in `apps/worker/src/ingestion/agent-scope.ts`): 3 canonicals — `assistant/recurring-themes`, `assistant/preferences-noted` (**load-bearing**: rewrote description to make this the operational reference for HOW to engage this specific user — covers both inferred patterns AND explicit user instructions like "be more concise"; complements user-scope `profile/preferences` which is the user-visible stated record), `assistant/open-questions`. Plus 2 non-canonical examples: `assistant/strengths`, `assistant/blind-spots`. **Cut `assistant/observations`** (everything in agent-scope IS an observation — redundant; general observations now land directly on the `assistant` root). **Cut `assistant/growth-edges`** (nudges the agent toward coaching/evaluation, which isn't its role). **Flash prompt** got a new section "Profile sub-pages → propose on-demand when content matches" with the canonical vocabulary, mirroring the existing "todos/todo unconditional flag" pattern. **Agent-scope prompt** "Where observations land" rewritten to reflect on-demand-only. **Pro prompt** wiki-ontology updated. **Live Agent prompt** wiki-structure section trimmed to "use the preload to see what currently exists for THIS user." Existing users won't get the seed change retroactively — see TODO list below.
- ✴️ **Layer 2 — Expand preload with structural snapshot.** `apps/server/src/calls/preload.ts` now fetches top-level user-scope pages + their immediate children (depth 2, capped at 12 children/parent for token budget) and renders them as a "Wiki structure" section in the system-prompt preload block. Distinct from the existing "Recently active wiki pages" list — that surfaces *active areas*; this surfaces *shape*.
- ✴️ **Layer 3 — Live Agent prompt: ask-when-ambiguous about structure.** New "Wiki structure" section in `composeGenericScaffolding`. Codifies the type-bucket allow-list at the conversational layer, gives concrete patterns for when to ask the user where to file something (substantial new entity + ambiguous structure + posture allows interruption), when NOT to (Self-exploration, Capture, obvious cases), and how to phrase it (casual offer, not procedural). Whatever the user says becomes the structural decision via the transcript handoff.
- ✴️ **Layer 4 — Pro prompt: type-bucket allow-list + explicit-direction respect.** `apps/worker/src/ingestion/pro-fan-out.ts` routing rule 3 + hard-rules bullets updated. Type-bucket allow-list (`profile`, `todos`, `projects` only — never invent others). Explicit user structural direction in the transcript trumps Pro's heuristics. Mirrored in `specs/fan-out-prompt.md`.

Worker + server typecheck clean across all four layers. Shared package rebuilt.

**TODO before marking ✅:**
- Dogfood a generic call that creates a new project + sub-topics, verify (a) the project lands under `projects` and the sub-topics nest under it, NOT under invented type-buckets; (b) the agent asks at least once when structure is genuinely ambiguous; (c) explicit user direction is respected.
- One-off cleanup of the user's existing wiki — delete the spurious `Concepts`, `Places`, etc. top-level pages from prior incidents. Easiest path: manual delete via the Wiki UI (Notes UI, post-rename — see backlog).
- Existing-user `projects` seed: the user's own account is missing the `projects` root because the seed didn't include it at signup. Will need either a one-off insert (manual SQL or a backfill script) OR live-with-it-and-let-Pro-create-it-on-first-use. Decide before testing.

### Observability — Pro fan-out claim-level audit (NEW)

- ✴️ **Claim-level audit trail for Pro fan-out** — Code shipped 2026-05-06, **field-test pending**. Chose Option B (JSON column on `call_transcripts`) over Option A (dedicated `extracted_claims` table) for v1: cheaper to ship, no Pro prompt change required, schema-agnostic to query patterns we don't yet know we want. Migration to A remains a clean follow-up once eval queries clarify the schema. Changes: (a) new column `call_transcripts.pro_fan_out_response jsonb` (nullable; server-only, never synced to mobile via RxDB) added in `packages/shared/src/db/schema/calls.ts`. Migration `apps/server/drizzle/0013_worthless_richard_fisk.sql` generated — also picks up accumulated schema drift unrelated to this work (`ingestion_status` enum + columns, `tombstoned_at` on user_settings, `title`/`citations` on research_outputs); harmless given the planned drop+reseed. (b) New module `apps/worker/src/ingestion/redact.ts` — regex-based PII redaction (email, SSN, phone, credit-card patterns) walking the JSON tree recursively. Names + project titles + general claim content intentionally preserved (redacting them would gut debugging value — the 2026-05-02 incident was solvable specifically because the claim text was inspectable). NER-based redaction deferred to V1+ if/when audit data leaves the developer's eyeline. (c) `apps/worker/src/ingestion/commit.ts` now persists Pro's structured response (`creates`/`updates`/`skipped`/`tasks`) to the new column inside the same transaction as the rest of the commit, after passing through the redactor. Failure of either persist path rolls back the whole commit (atomic with the wiki writes themselves). (d) Mobile RxDB unchanged — `call_transcripts` was never synced to mobile so nothing extra needed for server-only scope. **TODO before marking ✅:** dogfood by running an ingestion and inspecting `call_transcripts.pro_fan_out_response` for a real call; verify (i) the JSON contains expected creates/updates/skipped, (ii) PII redactions fire on test patterns, (iii) the column doesn't show up in mobile RxDB. Source: 2026-05-02 incident + 2026-05-06 implementation pass.

---

## Out of scope (explicitly deferred to V1)

These items came up while scoping the patch and are tagged here so future-Claude doesn't re-suggest them as polish work:

- **Theme switcher + light-mode toggle** (P1, M) — Five Liquid Glass variants tokened from MVP, but the picker UI + light-mode variants are V1 work, not polish. Would expand the scope of this patch beyond "smooth what's already there."
- **In-call transcript feed** (P2, M) — New surface, not polish.
- **Add-plugin tile / plugin marketplace surface** (P3, XL) — Tied to runtime plugin installation; a feature, not polish.
- **Wiki / CRUD / Todos / Projects / Graph / Activity-stream / Notifications / Call-history UI modules** — Each is a P0–P1 surface in its own right. The polish patch refines what's already on the home grid; new surfaces belong in V1 feature cycles.
- **Persona / agent config UX, in-call agent-config adjustment** — Feature-y, not polish.
- **Conflict resolution policy / offline behavior / initial hydration** — Sync infra, not UX.

---

## Demo target

A returning TestFlight user who used 0.1.0 should immediately notice:

1. The home grid feels "alive" — badges show new activity, the greeting reflects what's running.
2. Tapping the avatar opens settings (no more dead-end stub).
3. Plugin overlays dismiss with a swipe-up instead of accidentally backgrounding the app.
4. A research task in flight shows up in the Research overlay with a spinner instead of vanishing until done.
5. A failed research task surfaces visibly on its todo row with a retry button.
6. Editing a wiki section feels like editing rich text, not raw markdown.

If those six are true, the patch shipped.

---

## Open questions

- **Cycle length.** Roughly 1–2 weeks if WYSIWYG stays in scope; ~1 week if it splits out.
- **Agent_tasks → RxDB sync** is the only schema/RLS change required (for pending-artifact placeholders). Migration mirrors the `research_outputs` work in slice 6.5 — should be straightforward, but flag it now since it's the only non-client change.
- **Versioning.** EAS auto-increments build numbers; the marketing version (`0.1.1`) needs a manual bump in `app.json` before the first preview build. Stick on the `preview` channel for the patch; only promote to `production` once the user (Max) has dogfooded on a TestFlight install.
